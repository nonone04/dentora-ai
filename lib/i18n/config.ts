export const LOCALE_COOKIE_NAME = "dentora_locale";

/** One year -- a UI language preference should persist essentially indefinitely, same posture as next-themes' own localStorage persistence for theme. */
export const LOCALE_COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 365;
